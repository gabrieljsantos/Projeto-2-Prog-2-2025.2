#include <iostream>
#include <fstream>
#include "login_matricula.h"
#include "headers.h"

using namespace std;

namespace Login_mat {
    bool realizarLogin(int& id, string& nome, int& perfil) {
        system("cls || clear");
        cout << "Dentro de realizarLogin" << endl;
        // Lógica para realizar login
        return false;
    }
    void realizarMatricula() {
        system("cls || clear");
        cout << "Dentro de realizarMatricula" << endl;
        // Lógica para realizar matrícula
    }
}
